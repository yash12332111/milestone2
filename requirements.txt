# Mutual Fund FAQ Assistant — Dependencies
# ==========================================

# Core
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0

# Playwright (headless browser — used by GitHub Actions scraper only)
playwright>=1.40.0

# Vector Database (includes built-in ONNX embeddings — no PyTorch needed)
chromadb>=0.4.22

# LLM (Groq via openai-compatible client)
openai>=1.12.0
tiktoken>=0.6.0

# NLP
nltk>=3.8.0

# API Server (Phase 3)
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.6.0
